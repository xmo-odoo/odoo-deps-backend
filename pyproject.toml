[project]
name = "odoo-deps-backend"
version = "0.1.1"
authors = [
    { name = "Xavier Morel", email = "xmo@odoo.com" }
]
requires-python = ">=3.10"
dependencies = ["setuptools"]

[build-system]
requires = ["uv_build>=0.9.25,<0.10.0"]
build-backend = "uv_build"
